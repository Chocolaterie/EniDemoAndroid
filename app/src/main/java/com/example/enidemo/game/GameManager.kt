package com.example.enidemo.game

class GameManager {

    fun run(){
        var gachaManager = Actor();

        var humain = Humain();
        var bird = Bird()

        // Appel
        // var result = gachaManager.getComponentByClass<GachaComponent>().lotery(10)

        // Modal
        // result.rewardItems


        /*
        humain.getComponentByClass<ClasseComponent>().getAttribue("hp")

        humain.getComponentByClass<BodyComponent>().arms

        bird.getComponentByClass<ClasseComponent>().hp

        humain.getComponentByClass<EquipementComponent>().equipped

        humain.getComponentByClass<InventoryComponent>().add()
        humain.getComponentByClass<InventoryComponent>().add()

        bird.getComponentByClass<InventoryComponent>().add()
        */

    }
}